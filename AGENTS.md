# Unity プロジェクト作業方針

- `.unity`、`.prefab`、`.asset`、`.meta` など、Unity 独自形式のファイルは基本的に変更しない。
- C# コードを新たに作成する必要がある場合は、作成前に必ずユーザーの許可を得る。
- ユーザーが「やりたいこと」を尋ねた場合、原則としてコードの実装ではなく、Unity エディター上での操作方法を知りたいものとして扱う。
- 大まかな関数や処理はすでに用意されている前提で、Unity の操作に不慣れなユーザー向けに、Inspector、GameObject、コンポーネント設定などの具体的な手順を案内する。
